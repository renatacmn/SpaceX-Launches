project.ext {

    /* Config */
    applicationId = "br.com.spacexlaunches"
    compileSdkVersion = 28
    minSdkVersion = 19
    targetSdkVersion = 28
    versionCode = 1
    versionName = "1.0.0"

    /* Dependencies */

    // Android
    appCompatVersion = "1.1.0"

    // Google Architecture Components
    lifecycleVersion = "2.1.0"
    liveDataVersion = "2.1.0"
    viewModelVersion = "2.1.0"

    // Design
    materialVersion = "1.0.0"
    constraintLayoutVersion = "1.1.3"

    // Retrofit
    retrofitVersion = "2.6.1"
    okHttpVersion = "3.12.1"

    // Dagger
    daggerVersion = "2.24"

    // Lottie
    lottieVersion = "3.0.7"

    // Glide
    glideVersion = "4.9.0"

    // Test
    jUnitVersion = "4.12"
    testRunnerVersion = "1.2.0"
    espressoCoreVersion = "3.2.0"
}